# ChatGPT-First Control Plane

이 프로젝트의 사용자 인터페이스는 일반 ChatGPT 채팅이다.
사용자는 여기서 자연어로 원하는 결과를 말하고, ChatGPT가 GitHub와 GitHub Actions를 이용해 구현과 검증 루프를 운영한다.

## 표준 작업 루프

1. 일반 ChatGPT에서 사용자 요청을 받는다.
2. `AGENTS.md`, `WORKFLOW.md`, `MEMORY.md`, `REQUESTS.md`와 관련 코드를 읽는다.
3. 필요한 경우 공식 문서나 최신 자료를 조사한다.
4. 구현 방법을 선택하고 코드를 수정한다.
5. 변경을 GitHub에 커밋한다.
6. CI가 자동 실행된다.
7. 타입 검사, 단위 테스트, 빌드, Playwright 브라우저 테스트를 확인한다.
8. CI가 실패하면 실패 단계와 로그를 읽고 원인을 분류한다.
9. 같은 접근이 2회 실패하면 다른 전략으로 전환한다.
10. 브라우저 테스트 산출물과 스크린샷을 확인한다.
11. 필요한 수정 후 다시 CI를 통과시킨다.
12. 중요한 변경은 `CHANGELOG.md`, 장기 결정은 `MEMORY.md`에 기록한다.
13. 사용자에게는 최종 결과와 꼭 필요한 결정만 전달한다.

## ChatGPT가 직접 처리해야 하는 것

- 저장소 파일 읽기와 수정
- 테스트 실패 원인 분석
- GitHub Actions 상태 및 로그 확인
- 구현 방법 선택
- 작은 UI/UX 개선
- 브라우저 테스트 결과 검토
- 반복 실패 시 전략 변경

## 사용자 확인이 필요한 것

- 유료 API나 서비스 사용
- 로그인, 비밀키, 민감 인증정보
- 중요한 파일 또는 데이터 삭제
- 보안상 위험한 권한 확대
- 되돌리기 어려운 외부 시스템 변경

## 실행 계층

현재 기본 실행 계층은 GitHub Actions다. 웹 애플리케이션의 빌드와 자동 브라우저 테스트는 Actions에서 실행한다.

향후 일반 ChatGPT에서 로컬 PC의 터미널, 파일, 브라우저 조작이 공식적으로 안전하게 연결될 수 있으면 별도의 로컬 실행 브리지를 추가한다. 이 연결은 사용자 인터페이스를 바꾸지 않는다. 사용자는 계속 일반 ChatGPT에서만 지시한다.

## 완료 기준

작업은 코드 작성만으로 완료되지 않는다. 최소한 다음 조건을 만족해야 한다.

- 타입 검사 통과
- 단위 테스트 통과
- 프로덕션 빌드 통과
- Playwright 브라우저 테스트 통과
- 필요한 경우 데스크톱/모바일 스크린샷 생성
- GitHub Actions 최종 성공

CI는 `workflow_dispatch`도 지원하므로 필요할 때 동일 검증을 수동으로 다시 실행할 수 있다. 각 실행은 `browser-test-artifacts` 안에 브라우저 결과와 `ai-run/summary.json`을 남긴다.
