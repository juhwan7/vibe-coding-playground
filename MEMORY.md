# Project Memory

## 프로젝트 성격
- 취미용 바이브코딩 프로젝트
- 완성 속도만큼 다양한 실험과 재미를 중요하게 생각한다.
- 사용자는 중간 확인을 최소화하고 결과 중심으로 확인하는 방식을 선호한다.
- 실패 시 동일한 방법을 계속 반복하지 않고 다른 전략을 적극적으로 시도한다.
- 프로젝트에 어울리는 작은 개선은 AI가 자율적으로 추가할 수 있다.

## 고정 안전 규칙
- 유료 서비스 사용 전 확인
- 계정/인증 정보 필요 시 확인
- 중요한 데이터 삭제 전 확인
- 비밀키와 토큰은 Git에 저장하지 않음

## 장기 기술 결정
- 한국 주식 Money Flow 데이터 소스는 우선 토스증권 Open API를 사용한다.
- 사용자 화면에서는 KRX와 NXT를 굳이 분리하지 않고 통합 시장으로 표시한다.
- 거래 시간축은 NXT를 포함해 08:00~20:00으로 유지한다.
- 장중 시장 데이터와 테마 평균 데이터는 기본 1분 주기로 최신화한다.
- 거래대금 TOP100과 TOP50 테마 선정은 토스 시장 거래대금 랭킹의 1일 누적 기준을 사용한다.
- 시장 스냅샷은 TOP100 전체를 1분 단위로 Docker 영속 볼륨에 저장하며, 최근 데이터는 재시작 후에도 읽어 비교에 사용한다.
- 테마 평균 차트 구성종목의 1분봉도 Docker 영속 볼륨에 저장하고, 재시작 시 복원한다. 전일 전체 데이터가 부족하면 과거 캔들 API로 전일+오늘 구간을 자동 백필한다.
- 테마 차트는 TOP50 안에 같은 테마가 3종 이상일 때만 만들고, 테마별 1일 누적 거래대금 합계 순으로 정렬한다.
- 테마 평균은 각 구성종목 가격을 공통 기준점 대비 수익률로 정규화한 뒤 3분 단위 단순평균한다. 별도의 매수/매도 해석 문구는 넣지 않는다.
- 첫 화면은 `증시 자금` 페이지로 두고, 실시간 거래대금/수급과 투자자예탁금·CMA·신용융자·미수금 같은 일별 주변자금을 분리해 보여준다.
- 증시 주변자금의 공식 소스는 금융위원회/금융투자협회 종합통계 공공데이터(data.go.kr 15094809)를 우선 사용한다. 서비스키가 연결되기 전에는 해당 값을 추정하거나 가짜로 채우지 않는다.
- API 비밀키는 프론트엔드나 Git에 넣지 않고 Node.js 백엔드 환경변수에서만 사용한다.
- 로컬 실행은 Docker Compose로 프론트엔드와 백엔드를 함께 띄우며 Windows에서는 `START_WINDOWS.bat`을 기본 진입점으로 사용한다.
- GitHub Pages는 정적 DEMO FALLBACK용이며, 실제 토스 데이터는 비밀키와 허용 IP가 있는 백엔드가 연결된 환경에서만 제공한다.
- 현재 섹터/테마는 대표 종목 중심이며 전체 상장 종목 매핑은 후속 확장 과제로 둔다.
