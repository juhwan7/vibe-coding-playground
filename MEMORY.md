# Project Memory

## 프로젝트 성격
- 취미용 바이브코딩 프로젝트다.
- 완성 속도만큼 다양한 실험과 재미를 중요하게 생각한다.
- 사용자는 일반 ChatGPT에서 자연어로 요청하고 GitHub 수정·테스트·배포까지 최대한 자동으로 끝내는 방식을 선호한다.
- 실패 시 동일한 방법을 반복하지 않고 원인을 확인한 뒤 다른 전략을 시도한다.
- 프로젝트에 어울리는 작은 개선은 기존 기능과 데이터 정확성을 해치지 않는 범위에서 자율적으로 추가할 수 있다.

## 고정 안전 규칙
- 유료 서비스 사용 전 사용자 확인
- 계정/인증 정보 필요 시 확인
- 중요한 데이터 삭제 전 확인
- `.env`, API Secret, 토큰 등 비밀정보는 Git에 저장하지 않음
- 시장 데이터가 없을 때 가짜 수치로 채우지 않음

## 장기 기술 결정
- 한국 주식 Market Flow의 주 시세/거래대금 데이터 소스는 우선 토스증권 Open API를 사용한다.
- 사용자 화면에서는 KRX와 NXT를 불필요하게 분리하지 않고 통합 시장 관점으로 표시한다.
- 시장 감시 시간축은 NXT를 포함해 08:00~20:00을 기본으로 한다.
- 핵심 국내 시장 스냅샷은 장중 약 10초 주기로 갱신하고, 상대적으로 무거운 상세 수급은 더 느린 주기로 분산 수집한다.
- 거래대금 TOP100과 테마 후보는 토스 시장 거래대금 랭킹의 당일 누적(`duration=1d`)을 기본으로 사용한다.
- 시장 스냅샷과 히스토리는 Docker 영속 볼륨에 저장해 재시작 후에도 마지막 정상값과 이전 흐름을 복원한다.
- 테마 구성종목 1분봉도 영속 저장하고, 전일 데이터가 부족하면 과거 캔들 API로 자동 백필한다.
- 국내 최종 주도테마는 **5개를 유지하는 것이 현재 제품 규칙**이다. Market Intelligence 계층도 5개를 기준으로 한다.
- 작은 테마 순위 변화로 화면이 자주 교체되지 않도록 기본 8% 우위 또는 3회 확인 히스테리시스를 유지한다.
- 국내 테마 카탈로그는 `backend/data/themes.kr.json`에서 검증 가능한 형태로 관리한다. 현재 `광통신`을 포함한다.
- 테마 차트는 구성종목의 공통 기준점 대비 수익률을 사용하되 **3분 구간 거래대금을 가중치로 사용한다.** 거래대금이 큰 종목의 방향이 테마 평균선에 더 크게 반영되어야 한다.
- 모든 거래대금 가중치가 없거나 0인 특수 구간에서만 동일가중 평균으로 fallback한다.
- 테마 차트의 Y축은 실제 퍼센트를 변경하지 않는 범위에서 강하게 자동 확대해 작은 변동도 눈에 보이게 한다.
- 테마 구성종목을 클릭하면 실제 저장/백필한 1분봉을 3분 선차트로 보여주고 약 5초 후 원래 테마 차트로 복귀한다. 차트 데이터를 임의 생성하지 않는다.
- 국내 거래대금 TOP100의 종목명은 랭킹 응답만 신뢰하지 않고 별도 종목 메타데이터와 결합한다. 종목 메타 요청은 과도한 URL/개수 제한을 피하기 위해 작은 배치로 나눈다.
- 6자리 숫자 종목코드는 종목명으로 인정하지 않는다. 실제 이름이 준비되지 않은 순간에는 `종목명 확인 중`처럼 명시한다.
- 미국 테마 유니버스는 미국 거래대금 랭킹에서 **ETF·ETN 등 상장지수상품을 먼저 제거한 개별주 TOP50**으로 고정한다. TOP51 이후 종목은 테마 구성이나 부족 슬롯 보강에 사용하지 않는다.
- 미국 사용자 화면의 주도 테마는 **거래대금 합계 순 5개를 유지**한다. TOP50 안에서 3종 이상 모인 테마를 우선하고, 5개가 부족할 때만 같은 TOP50 안의 2종·1종 테마로 보강한다.
- 미국 테마 중앙 평균 차트도 국내 테마와 같은 방식으로 구성종목을 기준점 대비 정규화한 뒤 **3분 거래대금 가중 수익률**을 사용한다. 모든 3분 거래대금이 0인 특수 구간에서만 동일가중 평균으로 fallback한다.
- 미국 테마 흐름은 국내 테마 흐름과 같은 SaaS 시각 언어를 사용하고 정규장 1분봉을 3분 단위로 집계하며 ET/KST를 함께 표시한다.
- 미국 전용 뉴스 이벤트 API가 연결되기 전에는 뉴스·재료를 임의 생성하지 않는다. 미국 테마 차트 이벤트는 실제 수집된 3분 데이터에서 확인되는 거래대금 피크와 상승 피크만 표시한다.
- 국내/미국 SaaS 보조 로직은 페이지별로 분리해 국내 데이터나 DOM 정리 로직이 미국 화면에 섞이지 않도록 유지한다.
- 시황 요약의 당일 시작점은 **06:00 KST**다.
- 뉴스는 매 갱신 때 06:00부터 현재까지를 다시 검색해 오전에 놓친 기사도 backfill할 수 있게 하고, 이미 찾은 당일 비중복 후보는 유지한다.
- 시황 요약은 거래대금 상위 종목뿐 아니라 현재 주도테마와 수주·계약·투자·증설·정책·승인·실적 같은 테마 촉매, 그리고 CPI/FOMC/환율/유가/지정학 같은 시장 영향 뉴스를 포함한다.
- 광고·리딩방·저가치 기사와 같은 사건의 반복 기사는 제거/통합한다.
- 뉴스 근거는 1차자료/복수출처/단일기사 수준을 구분하고, 뉴스와 주가의 시간적 인접성을 인과관계라고 단정하지 않는다.
- `금일 이슈 정리`는 데스크톱에서 **왼쪽 이슈 목록 / 오른쪽 선택 종목 차트 50:50**을 기본으로 한다. 우측 차트는 전일+오늘 **실제 1분봉만 수집해 5분 종가로 압축**하며, 거래일 사이와 15분 초과 데이터 공백은 선으로 연결하거나 보간하지 않는다.
- 첫 화면과 상단 메뉴의 최우선 화면은 `국내 테마 흐름`으로 두고, `MARKET FLOW` 브랜드를 눌러도 국내 테마 흐름으로 돌아온다.
- 국내 테마 흐름 화면에서는 테마 강도·TOP100 시장 지도를 먼저 보여주고, `장중 시황 브리핑`은 우측 서랍에 기본 숨김 상태로 두어 필요할 때만 열어본다. 숨김 상태에서는 브리핑 컴포넌트를 마운트하지 않아 불필요한 추가 조회도 피한다.
- `증시 자금`은 별도 메뉴로 유지하며 실시간 거래대금/수급과 투자자예탁금·CMA·신용융자·미수금 같은 일별 주변자금을 구분한다.
- 증시 주변자금은 금융위원회/금융투자협회 종합통계 공공데이터를 우선 사용한다. 서비스키/실데이터가 없으면 추정값으로 채우지 않는다.
- API 비밀키는 프론트엔드나 Git에 넣지 않고 Node.js 백엔드 환경변수에서만 사용한다.
- 로컬 실행은 Docker Compose를 사용하고 Windows에서는 `START_WINDOWS.bat`을 기본 진입점으로 둔다.
- 실제 공개 앱은 Raspberry Pi + Nginx + Node backend + Cloudflare Quick Tunnel 구조이며 Quick Tunnel 주소는 회전할 수 있다. 최신 URL은 `tunnel-status/CURRENT_TUNNEL.md`를 기준으로 확인한다.
- GitHub-hosted CI가 프론트엔드를 빌드하고, 성공한 `main`의 검증된 artifact/SHA만 Raspberry Pi self-hosted runner에 배포한다.
- 전체 상장종목에 대한 완전한 테마 매핑은 아직 후속 확장 과제이며, 현재는 검증된 테마/종목 카탈로그를 우선한다.
