# AI Autonomous Development Rules

이 저장소는 취미용 바이브코딩 프로젝트다.
사용자는 **일반 ChatGPT 채팅 한 곳에만 자연어로 요청**하고, ChatGPT가 GitHub와 자동 테스트를 이용해 구현·검증·수정·개선을 최대한 자율적으로 수행한다.

## 최우선 운영 원칙
1. 사용자의 주 인터페이스는 일반 ChatGPT다. 사용자를 Codex, 터미널, GitHub Actions 화면으로 보내는 것을 기본 흐름으로 삼지 않는다.
2. 현재 채팅에서 GitHub 저장소를 읽고 수정할 수 있으면 직접 처리한다.
3. 코드 변경 후 GitHub Actions 결과를 직접 확인하고, 실패하면 로그를 읽어 원인을 수정한다.
4. 사용자에게 중간 진행 보고를 최소화한다. 가능한 작업은 묻지 말고 진행하고, 최종 결과나 정말 필요한 결정만 알린다.
5. `Codex`는 사용자가 직접 명령해야 하는 필수 인터페이스가 아니다. 필요할 경우 내부 작업 수단으로만 취급한다.

## 작업 시작 순서
1. `WORKFLOW.md`를 확인한다.
2. `REQUESTS.md`와 `MEMORY.md`를 확인한다.
3. 현재 코드 구조와 최근 변경을 확인한다.
4. 요청을 작은 작업으로 분해하되, 사소한 선택은 사용자에게 묻지 않는다.
5. 필요한 경우 최신 공식 문서나 신뢰할 수 있는 자료를 조사한다.

## 기본 원칙
1. 사용자의 요청을 문자 그대로만 구현하지 말고, 더 재미있고 편리한 개선이 명확하면 작은 범위에서 자율적으로 추가할 수 있다.
2. 큰 변경 전에는 Git으로 되돌릴 수 있는 상태를 유지한다.
3. 작업 완료 후 `CHANGELOG.md`를 갱신하고, 장기적으로 중요한 결정만 `MEMORY.md`에 남긴다.
4. 유료 API/서비스, 계정 인증, 중요한 데이터 삭제, 보안 위험이 있는 작업만 사용자 확인을 요구한다.
5. 가능하면 GitHub Actions와 Playwright 산출물로 실행 결과를 검증한다.

## 실패 전략
같은 해결책을 무한 반복하지 않는다.

- 동일한 접근의 수정 시도는 최대 2회.
- 같은 원인으로 다시 실패하면 현재 전략을 중단하고 원인을 재분석한다.
- 다음 순서로 다른 전략을 시도한다.
  1. 오류 메시지와 최소 재현 조건 분석
  2. 공식 문서 확인
  3. 구현 방법 또는 라이브러리 변경 검토
  4. 문제를 더 작은 단위로 분리
  5. 최소 프로토타입에서 독립 검증
  6. 성공한 방법만 본 코드에 통합
- 여러 독립 전략이 실패했을 때만 사용자에게 알린다.
- 단순히 실패한 동일 명령을 반복 실행하는 것은 해결 전략으로 세지 않는다.

## 자동 검증 루프
가능한 경우 다음 순서로 검증한다.
1. 정적 검사 / 타입 검사
2. 단위 테스트
3. 프로덕션 빌드
4. 실제 브라우저 기능 테스트
5. 데스크톱/모바일 스크린샷 생성
6. GitHub Actions 최종 상태 확인

CI가 실패하면:
`실패 단계 확인 → 로그 확인 → 원인 분류 → 코드/설정 수정 → 새 커밋 → CI 재검증`

## UI/UX 자율성
다음은 기존 기능과 성능을 해치지 않는 범위에서 자율적으로 실험할 수 있다.
- 애니메이션
- 마이크로 인터랙션
- 3D/깊이감
- 패럴랙스
- 작은 편의 기능
- 접근성 개선
- 이스터에그
- 최신 웹 기능

새로운 시각적 구현은 가능하면 작은 프로토타입으로 먼저 검증한 뒤 확대한다.

## 기술 선택
사용자가 기술을 명시하지 않았다면 다음 기준으로 조사·선택한다.
1. 구현 단순성
2. 유지보수성
3. 성능
4. 공식 지원 상태
5. 무료 사용 가능 여부
6. 실험성과 재미

새 기술을 도입하기 전에 공식 문서를 우선 확인한다.

## 사용자에게 반드시 확인할 것
- 결제 또는 유료 API 사용
- 계정 로그인/민감한 인증정보 입력
- 중요한 데이터나 파일의 대량 삭제
- 보안상 위험한 권한 확대
- 서로 다른 결과 중 취향 선택이 프로젝트 방향을 크게 바꾸는 경우
- 여러 독립적인 해결 전략을 시도했는데도 해결하지 못한 경우

## 금지
- 같은 실패를 의미 없이 반복
- `.env`, 비밀키, 토큰을 저장소에 커밋
- 사용자의 승인 없는 유료 서비스 사용
- 정상 동작 중인 핵심 기능을 이유 없이 대규모 재작성
- 사용자가 일반 ChatGPT만 사용하길 원하는데 불필요하게 다른 도구에서 직접 명령하라고 요구
