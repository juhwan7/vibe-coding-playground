# AI Autonomous Development Rules

이 저장소는 취미용 바이브코딩 프로젝트다.
사용자는 방향과 아이디어를 자연어로 지시하고, AI가 구현·테스트·수정·개선을 최대한 자율적으로 수행한다.

## 기본 원칙
1. 작업 시작 시 `REQUESTS.md`, `MEMORY.md`, 현재 코드 구조를 먼저 확인한다.
2. 사용자의 요청을 문자 그대로만 구현하지 말고, 더 재미있고 편리한 개선이 명확하면 작은 범위에서 자율적으로 추가할 수 있다.
3. 사소한 기술 선택은 사용자에게 묻지 않는다.
4. 큰 변경 전에 Git으로 되돌릴 수 있는 상태를 유지한다.
5. 작업 완료 후 `CHANGELOG.md`를 갱신하고, 장기적으로 중요한 결정만 `MEMORY.md`에 남긴다.
6. 유료 API/서비스, 계정 인증, 중요한 데이터 삭제, 보안 위험이 있는 작업만 사용자 확인을 요구한다.

## 실패 전략
같은 해결책을 무한 반복하지 않는다.

- 동일한 접근의 수정 시도는 최대 2회.
- 같은 원인으로 다시 실패하면 현재 전략을 중단하고 원인을 재분석한다.
- 다음 순서로 다른 전략을 시도한다.
  1. 오류 메시지와 최소 재현 조건 분석
  2. 공식 문서 확인
  3. 구현 방법 또는 라이브러리 변경 검토
  4. 문제를 더 작은 단위로 분리
  5. 최소 프로토타입에서 독립 검증
  6. 성공한 방법만 본 코드에 통합
- 여러 독립 전략이 실패했을 때만 사용자에게 알린다.

## 자동 테스트
가능한 경우 다음 순서로 검증한다.
1. 정적 검사 / 타입 검사
2. 단위 테스트
3. 빌드
4. 브라우저 기능 테스트
5. 시각적 확인용 스크린샷

테스트 재실행 자체를 해결책으로 취급하지 않는다.

## UI/UX 자율성
다음은 기존 기능과 성능을 해치지 않는 범위에서 자율적으로 실험할 수 있다.
- 애니메이션
- 마이크로 인터랙션
- 3D/깊이감
- 패럴랙스
- 작은 편의 기능
- 접근성 개선
- 이스터에그
- 최신 웹 기능

## 기술 선택
사용자가 기술을 명시하지 않았다면 다음 기준으로 조사·선택한다.
1. 구현 단순성
2. 유지보수성
3. 성능
4. 공식 지원 상태
5. 무료 사용 가능 여부
6. 실험성과 재미

새 기술을 도입하기 전에 공식 문서를 우선 확인한다.

## 금지
- 같은 실패를 의미 없이 반복
- `.env`, 비밀키, 토큰을 저장소에 커밋
- 사용자의 승인 없는 유료 서비스 사용
- 정상 동작 중인 핵심 기능을 이유 없이 대규모 재작성
