# Changelog

## 0.6.11
- 국내 테마 평균 차트는 KST 기준 **오늘 데이터만** 표시해 전일 구간이 한 화면에 함께 섞이지 않도록 변경
- 국내 테마 차트와 종목 3분 미리보기의 X축을 **08~20시 1시간 간격**으로 통일하고 `:00` 분 표기를 제거
- 시간 눈금 글씨를 키워 데스크톱/고해상도 화면에서 장중 시각을 더 쉽게 읽도록 개선
- 거래대금 TOP100 수집 단계에서 랭킹 응답의 종목명에만 의존하지 않고 Toss 종목 메타데이터를 **10종목 단위로 실제 조회해 결합**하도록 수정
- 묶음 종목 메타 응답에서 이름이 누락되거나 요청이 일부 실패하면 해당 종목을 개별 조회로 재시도하고, 반복 실패 시 1분 뒤 다시 시도하도록 복구 경로 추가
- 메타데이터의 `securityType`도 개별주 필터에 반영하고 종목명 누락→개별 재조회 복구를 백엔드 회귀 테스트로 추가

## 0.6.10
- `금일 이슈 정리`의 일봉 범위를 최근 30거래일에서 **최근 60거래일**로 확장
- 기존 고가↔저가 세로선 + 시가/종가 좌우 눈금 형태를 일반적인 증권 차트처럼 **고가↔저가 꼬리 + 시가↔종가 직사각형 몸통**을 쓰는 표준 캔들(봉) 차트로 변경
- 60거래일을 한눈에 보기 쉽도록 가격 격자를 5단계로 늘리고 날짜 눈금도 시작·25%·중간·75%·마지막 위치에 표시
- 일봉은 토스증권 Open API의 실제 OHLC만 사용하고 누락 거래일을 임의 보간하거나 가짜 봉으로 생성하지 않는 원칙 유지
- 금일 이슈 캐시를 `schemaVersion 6`으로 올려 기존 30일 캐시를 자동 무효화하고 배포 후 실제 60거래일 데이터를 다시 수집
- 백엔드에서 기본 60개 일봉 보존을, Playwright에서 60일 안내·캔들 몸통 렌더링·종목 전환을 회귀 검증

## 0.6.9
- 데스크톱/4K 레이아웃은 그대로 유지하고 **760px 이하에서만 적용되는 모바일 전용 반응형 레이어**를 추가
- 모바일 상단 6개 메뉴를 별도 행의 가로 스크롤 탭으로 바꿔 메뉴·새로고침·테마 전환 버튼이 서로 겹치거나 잘리지 않도록 개선
- 국내/미국 테마 요약, 테마 카드, 차트 지표, TOP100, 증시 자금, 금일 이슈, 시장 복기, 종목 퀴즈의 열 폭·패딩·글자 크기·줄바꿈을 좁은 화면에 맞게 재조정
- 390px 이하에서는 주요 통계·카드·차트 지표를 더 적극적으로 1열로 전환하고, 페이지 전체 가로 스크롤 대신 메뉴·리더 칩처럼 필요한 요소만 내부 스크롤을 허용
- 360px viewport에서 주요 6개 화면을 순회하며 `document` 가로 overflow와 주요 컨테이너의 viewport 이탈을 검사하는 Playwright 회귀 테스트 및 모바일 스크린샷 검증 추가

## 0.6.8
- `금일 이슈 정리` 데스크톱 50:50 배치를 **왼쪽 선택 종목 차트·상세 / 오른쪽 TOP100 이슈 목록**으로 서로 교체
- 기존 1분 종가 선차트를 토스증권의 실제 `시가·고가·저가·종가`를 그대로 사용하는 **1분 OHLC 막대차트**로 변경
- 1분 차트 위에 선택 종목의 **최근 30거래일 실제 일봉 OHLC** 미니 차트를 추가해 단기 장중 흐름과 최근 일봉 위치를 한 화면에서 함께 확인 가능
- OHLC 막대는 세로선으로 고가↔저가, 왼쪽 눈금으로 시가, 오른쪽 눈금으로 종가를 표시하고 상승은 빨강·하락은 파랑으로 구분
- 당일 일봉은 종가 확정값으로 오해하지 않도록 **15:20 스냅샷**임을 명시하고, OHLC가 누락된 데이터는 임의 보간·생성하지 않음
- 일봉/1분봉 실제 OHLC를 저장하도록 금일 이슈 캐시를 `schemaVersion 4`로 갱신해 기존 종가 전용 캐시를 자동 무효화
- 백엔드에서 2거래일 1분 OHLC 보존·최근 일봉 선택을 회귀 테스트하고, Playwright에서 좌우 패널 순서·일봉/분봉 막대·종목 선택을 검증

## 0.6.7
- 국내 핵심 시장 수집 시간을 **08:00~20:00 KST**로 명시하고 20:00 이후에는 고빈도 수집을 중단하도록 세션 경계를 정밀화
- NXT 애프터마켓이 시작되는 **15:30~20:00**에는 기존 `duration=1d` 거래대금 랭킹과 `duration=realtime` 랭킹을 함께 조회
- 두 랭킹 값을 더하거나 추정하지 않고 종목별 거래대금·거래량의 **더 큰 실제 관측값**을 채택해 realtime 값이 짧은 구간 값일 경우 기존 누적값을 훼손하지 않도록 처리
- 국내 테마 차트는 정규장 실제 1분봉 이후 NXT 구간에서 **실제 현재가 관측 표본**을 1분 단위로 누적하고, 거래대금 가중치는 직전 누적값 대비 실제 증가분만 사용
- 첫 관측·누적값 역행 시 가중치를 0으로 두고 임의 보간이나 가짜 거래대금을 생성하지 않도록 처리
- NXT 관측 표본과 누적 기준값을 Pi 테마 캐시에 영속 저장해 백엔드 재시작 후에도 당일 애프터마켓 차트 흐름을 이어갈 수 있도록 확장
- 15:30 시작/20:00 종료, `1d`·`realtime` 병합, SK하이닉스형 누적 거래대금 증가, 실제 관측 표본 증가분 계산을 백엔드 회귀 테스트로 고정

## 0.6.6
- `금일 이슈 정리` 데스크톱 화면을 **왼쪽 TOP100 이슈 목록 / 오른쪽 선택 종목 장중 차트 50:50** 구조로 개편
- 종목 행을 선택하면 우측 대형 차트와 금일 이슈 상세가 해당 종목으로 즉시 전환되도록 상호작용 추가
- 우측 차트는 토스증권의 **전일+오늘 실제 1분봉**을 `before` 페이지네이션으로 백필하고 화면에서는 실제 5분 종가로 압축해 표시
- 거래일 사이와 15분을 초과하는 데이터 공백은 선을 끊어 실제 데이터가 없는 구간을 임의 보간하거나 가짜 사선으로 연결하지 않도록 처리
- 기존 당일 단일 차트 캐시를 schemaVersion 2로 갱신해 배포 후 2거래일 구조로 다시 생성하도록 변경
- 좁은 화면에서는 대형 차트를 목록 위에 배치하는 단일 열 반응형 레이아웃 적용
- 2거래일 분리/5분 압축 백엔드 테스트와 50:50 레이아웃·날짜 표시·종목 선택 Playwright 회귀 테스트 추가

## 0.6.5
- 미국 테마 유니버스를 미국 거래대금 랭킹에서 ETF·ETN 등 상장지수상품을 먼저 제거한 **개별주 TOP50**으로 변경
- 미국 주도 테마를 거래대금 합계 순으로 **5개 고정**하고, 3종 이상 테마를 우선하되 슬롯이 부족해도 TOP50 밖 종목은 사용하지 않도록 제한
- 미국 테마 카탈로그를 `클라우드·데이터센터`, `소비·유통`, `미디어·스트리밍` 등으로 보강해 TOP50에서 실제 주도군을 더 잘 분류하도록 개선
- 미국 중앙 테마 평균선을 단순 동일가중에서 국내 테마 흐름과 같은 **3분 거래대금 가중 수익률**로 변경
- 미국 우측 거래대금 랭킹과 상단 시장 확산도·거래대금 합계도 ETF/ETN 제외 개별주 TOP50 기준으로 통일
- TOP51 이후 종목이 미국 테마에 섞이지 않는지, ETF/ETN 필터와 5개 테마 선택, 거래대금 가중 평균을 백엔드 테스트로 고정
- 데스크톱·모바일 Playwright에서 `개별주 TOP50`, `5개 고정`, `3분 거래대금 가중 평균` UI를 회귀 검증

## 0.6.4
- `미국 테마 흐름`에 국내 테마 화면과 같은 SaaS 디자인 언어를 적용하고 `US MARKET PULSE` 요약 바를 추가
- 미국 거래대금 TOP100을 기준으로 상승 종목 확산도와 전체 거래대금을 계산하고, 현재 주도 테마 TOP3와 1위 테마 내 최대 종목 거래대금 집중도를 함께 표시
- 미국 테마 1위를 `LEADER`로 강조하고 테마 강도에 따라 상승·보합·하락 배경 그라데이션을 옅게 적용
- 각 미국 테마에 최대 종목 거래대금 비중 게이지를 추가하고 우측 TOP100에는 실제 거래대금 Heat와 현재 주도 테마 색상·라벨을 연결
- 미국용 뉴스 이벤트 API가 없는 상태에서 재료를 임의 생성하지 않고 실제 3분 데이터의 `거래대금 피크`와 `상승 피크`만 차트 이벤트 마커로 표시
- 미국 테마 차트도 15분 초과 데이터 공백 또는 거래일 변경 시 선을 끊어 데이터가 없는 구간에 가짜 사선이 생기지 않도록 개선
- 국내 SaaS 보조 컴포넌트를 국내 페이지에서만 실행하도록 분리해 미국 페이지 전환 시 국내 데이터나 DOM 정리 로직이 미국 UI에 섞이지 않도록 수정
- 미국 SaaS 계산 단위 테스트와 데스크톱·모바일 Playwright 화면 검증을 추가

## 0.6.3
- 국내 테마 흐름의 10초 실시간 값 갱신 시 화면 곳곳이 번쩍이던 배경 점멸 효과를 제거해 숫자만 조용히 갱신되도록 개선
- 테마 거래대금 가중 3분 차트의 최신 위치에 세로 가이드, 강조 링, `현재` 수치 라벨을 추가해 현재 지점을 빠르게 찾을 수 있도록 개선
- 4K/초광폭 화면에서 상단 메뉴, 테마·구성종목, 차트 눈금, 거래대금 TOP100, 시장 상세 지표의 글자 크기와 명암을 키우는 전용 가독성 프로필 추가
- 우측 거래대금 TOP100 종목이 현재 왼쪽 5개 테마에 포함되면 같은 테마 색상의 외곽선·좌측 컬러 바·거래대금 미니바·테마 라벨로 연결해 소속 테마를 즉시 확인할 수 있도록 개선
- 한 종목이 여러 현재 테마에 동시에 포함되면 왼쪽에서 더 높은 순위의 테마 색상을 우선 적용해 시각적 충돌을 방지
- 현재 주도 5개 테마에 포함되지 않은 TOP100 종목도 검증된 전체 테마 카탈로그에 매칭되면 `자동차`, `인터넷·게임`, `2차전지`, `바이오` 같은 중립색 테마 라벨을 표시하고, 주도 5개 종목만 기존 테마색 테두리 강조를 유지
- TOP100 전체 테마 라벨은 기존 수동 검증 카탈로그만 사용하며 미등록 종목을 임의 추정하지 않도록 백엔드 회귀 테스트 추가

## 0.6.2
- 앱 첫 진입 화면과 `MARKET FLOW` 브랜드 버튼의 복귀 대상을 **국내 테마 흐름**으로 변경하고 상단 메뉴에서도 국내 테마 흐름을 첫 번째로 배치
- 국내 테마 화면에서 테마 강도·TOP100 시장 지도를 먼저 보여주도록 순서를 조정해 핵심 테마 흐름이 첫 화면에서 바로 보이도록 개선
- `장중 시황 브리핑`을 기본 화면에서 제거하고 우측 고정 버튼으로 여는 슬라이드 서랍으로 이동. 닫혀 있을 때는 컴포넌트를 마운트하지 않아 불필요한 추가 조회도 줄임
- ESC 키·배경 클릭·닫기 버튼을 지원하고 모바일에서는 우측 하단 플로팅 버튼과 전체폭 서랍으로 동작하도록 반응형 처리
- Playwright에서 국내 테마 기본 진입, 브리핑 기본 숨김/열기/닫기, 메뉴 순서와 증시 자금 접근성을 회귀 검증

## 0.6.1
- 국내 최종 테마 수를 Market Intelligence 계층까지 **5개**로 통일해 원시 테마는 5개인데 화면은 4개만 보이던 불일치 수정
- 검증 테마 카탈로그에 `광통신` 추가: 거래대금 상위권에 광통신 관련 종목이 함께 등장하면 독립 테마 후보로 집계 가능
- 테마 평균선을 동일가중 수익률에서 **3분 거래대금 가중 수익률**로 변경. 거래대금이 큰 종목의 상승/하락이 테마선에 더 크게 반영되도록 개선
- 10초 live overlay도 구성종목의 현재 누적 거래대금으로 가중해 실시간 테마 방향에 반영
- 테마선의 실제 퍼센트 값은 유지하면서 Y축 여백을 축소하고 차트 높이·선 굵기를 키운 `강한 자동 확대축` 적용
- 우측 거래대금 TOP100 종목명이 코드로만 보이던 문제를 구조적으로 보완: 종목 메타데이터를 25종목씩 나눠 조회하고 `name/stockName/displayName/shortName/koreanName`을 통합 처리
- 6자리 숫자 종목코드는 유효한 종목명으로 인정하지 않으며, 메타데이터 준비 전에는 `종목명 확인 중`으로 표시해 코드가 이름처럼 중복 노출되지 않도록 변경
- 시황 요약 기준을 **매일 06:00 KST 이후**로 확정하고, 매 3분 갱신 때 06:00부터 현재까지를 다시 검색하는 backfill 구조로 변경
- 오전에 놓친 중요 기사도 이후 재검색에서 발견되면 당일 후보 풀에 누적하고, 이미 수집한 비중복 기사도 유지
- 시황 검색 범위를 현재 거래대금 상위 종목뿐 아니라 현재 주도테마, 반도체/HBM/AI/데이터센터/광통신/원전/전력/방산/조선/바이오/로봇/2차전지 촉매로 확대
- 수주·공급·투자·증설·정책·승인·허가·실적·M&A 등 테마를 움직일 수 있는 기사를 `테마 이슈`로 포함하고 광고/리딩방/저가치·중복 기사는 계속 제거
- `backend/themeWeightedSeries.test.mjs`, `backend/stockMetadata.test.mjs`를 추가하고 5개 테마·광통신·06:00 경계·뉴스 누적·거래대금 가중 방향성을 회귀 테스트로 고정
- README를 현재 시스템 기준으로 전면 갱신하고 실제 Cloudflare Quick Tunnel 접속 주소와 최신 주소 레지스트리 링크 추가
- `PROJECT_STATUS.md`, `MEMORY.md`, `REQUESTS.md` 등 운영 문서를 5개 테마·거래대금 가중 차트·06:00 뉴스 정책에 맞게 동기화

## 0.6.0
- 국내 테마 흐름을 단순 거래대금 순위에서 규칙 기반 `Market Intelligence` 구조로 확장
- 테마는 사용자 화면에 항상 최대 4개를 유지하고, 내부 5번째 후보를 challenger로 사용해 기존 약한 테마보다 8% 이상 강하거나 3회 연속 우위를 확인할 때 교체하는 히스테리시스 적용
- 테마별 상승 확산도, 구성종목 중앙값 수익률, 대장주 거래대금 집중도, 10분 환산 자금 유입 속도, 종합 강도점수 추가
- 테마 생애주기를 `출현 / 확산 / 주도 / 과열 / 둔화 / 이탈 / 유지`로 원시값 기반 분류
- 여러 테마에 동시에 속한 종목의 거래대금을 1/N로 나누는 중복조정 거래대금 추가
- 국내 테마 종목/키워드 목록을 `backend/data/themes.kr.json`에서 검증 가능한 형태로 관리한다.
- 시장 스냅샷/테마/뉴스/선물에 `LIVE / DELAYED / STALE / FALLBACK / MISSING` 신선도 판정 추가
- TOP100 표본 부족, 누적 거래대금 역행, 비정상적으로 동일한 등락률 등 데이터 품질 이상 감지 추가
- 상승 종목 비율·상승 거래대금 비중·TOP10 집중도·외국인/기관/프로그램 수급·테마 확산을 조합한 규칙 기반 시장 상태 엔진 추가
- 외국인·기관·비차익·차익 프로그램의 장중 흐름 sparkline 추가
- 뉴스에 A/B/C 근거 등급 추가: A=공시·거래소 등 1차자료 확인, B=복수 출처, C=단일기사/미확인
- 주도 종목 뉴스 시각과 인접 가격을 한 타임라인에 표시해 뉴스와 주가의 선후관계를 확인하되 인과관계로 단정하지 않도록 변경
- 최근 2거래일 히스토리를 5분 단위로 복기하는 장중 Replay 슬라이더 추가
- 매 거래일 15:20 `PRE_CLOSE`, 15:35 `FINAL` 두 시점의 시장/테마/수급 상태를 35거래일 영속 저장하는 Close Archive 추가
- 검증된 KOSPI200 선물 공급원을 연결할 수 있는 `FUTURES_SNAPSHOT_URL` 인터페이스 추가. 공급원이 없으면 선물 값을 임의 생성하지 않고 `실데이터 미연결`로 표시
- 공개 `/api/market/refresh`에 기본 20초 서버 쿨다운을 적용하고, CORS wildcard를 제거해 명시적 `MARKET_ALLOWED_ORIGIN`이 있을 때만 Origin 허용
- `/api/market/intelligence`, `/api/market/event-timeline`, `/api/market/replay`, `/api/market/close-archive`, `/api/market/futures` 추가
- Nginx가 `market-intelligence.json` prepared snapshot을 직접 제공하도록 확장해 Pi의 페이지 조회 비용 최소화
- `RUNTIME_STATUS.md`를 별도로 게시하는 15분 주기 런타임 상태 워크플로 추가: Pi 로컬 앱 상태, 실제 배포 커밋, 터널 상태, 터널 레지스트리 갱신시각, 터널 age를 분리 기록
- `package-lock.json` 생성 및 CI를 `npm ci` 기반으로 전환해 의존성 재현성 강화
- Market Intelligence 신선도·뉴스 근거등급·테마 4개 제한·데이터 품질 이상 감지 백엔드 테스트 추가

## 0.5.1
- 국내 테마 흐름에서 Toss 종목 메타데이터의 `securityType=STOCK` 기준으로 ETF/ETN/리츠 등 비개별주 상품 제외
- 테마 강도 영역을 강한 4개 테마 중심으로 재정렬하고 더 강한 후보가 등장하면 약한 테마가 교체될 수 있도록 개선
- 특징주 이슈를 한국시간 당일 06:00 이후 기사만 사용하도록 제한하고 중복·홍보성 기사 필터 강화
- 특징주 이슈 카드를 2줄 가로 타임라인으로 변경하고 마우스 휠·드래그·터치 스크롤 지원
- 테마 움직임 차트를 막대 기반으로 변경하고 실제 값은 유지하면서 작은 변화도 구분할 수 있도록 자동 Y축 확대 적용
- 테마 종목 클릭 시 Pi의 저장된 1분봉 캐시를 3분 OHLC로 집계해 약 5초간 미리보기 후 자동 복귀
- PR #12의 전체 타입 검사·단위/백엔드 테스트·Docker 빌드·Playwright 검증 후 Raspberry Pi main 배포 완료
- 만료된 Cloudflare Quick Tunnel 때문에 정상 배포된 Pi가 외부에서 열리지 않는 경우를 감지하고 user-systemd 및 Docker `cloudflared` fallback으로 복구할 수 있도록 배포 운영 로직 강화
- `PROJECT_STATUS.md`를 추가해 프로젝트 목표, 데이터 구조, 배포 상태, 최근 완료 기능, 웹 인수 테스트 항목과 다음 작업을 새 Chat/Codex가 이어받을 수 있도록 정리

## 0.5.0
- 첫 화면에 `국내 증시 자금 상태` 페이지 추가: TOP100 누적 거래대금, 최근 1분 증가액, 과거 동시간 평균, 투자주체 현물 순매수, 프로그램 수급을 한 화면에 배치
- 투자자예탁금, CMA, 신용융자, 미수금, 신용/예탁금 비율 등 증시 주변자금 영역 추가
- 주변자금은 공식 금융위원회/금융투자협회 공공데이터 연결 전에는 값을 임의 생성하지 않고 `-` 및 데이터 소스 상태로 표시
- 토스 거래대금 랭킹을 `duration=1d` 기준으로 변경해 장중 순간값이 아니라 당일 누적 거래대금 TOP100을 사용
- 장중 시장 스냅샷과 테마 평균 데이터를 1분마다 최신화하도록 통일
- 시장 히스토리에 TOP100 전체를 1분 단위로 저장하고 35일 보관 구조 유지
- 테마 구성종목의 1분봉을 Docker 영속 볼륨에 저장하고 재시작 시 복원하도록 변경
- 캐시에 전일 전체 데이터가 부족하면 토스 과거 1분봉을 페이지네이션해 전일+오늘을 자동 백필
- 테마 차트의 거래량 막대를 3분 추정 거래대금 막대로 변경하고 정확한 1시간 간격 세로 눈금/시간 라벨 추가
- 각 테마 차트에 최대 3분 거래대금 발생 시각과 최대 3분 상승 발생 시각을 원시 비교값으로 표시
- 4K 화면에서 차트가 과도하게 길어지지 않도록 전체 작업공간 폭을 제한하고 정보 밀도를 재조정
- 화이트 테마 전용 고대비 색상 규칙을 추가해 제목·종목명·거래대금·보조문구·차트 눈금 가독성 개선
- 상단 메뉴를 `증시 자금 / 테마 흐름 / 종목 퀴즈`로 확장하고 증시 자금을 기본 첫 페이지로 지정

## 0.4.0
- 토스증권 Open API 기반 읽기 전용 시장 데이터 백엔드 추가
- OAuth Client Credentials 토큰 캐시, 401 재인증, 429 재시도 처리 추가
- KRX/NXT를 화면에서 분리하지 않는 통합 현재가·거래대금 구조 적용
- 토스 시장 전체 거래대금 TOP100 랭킹과 추적 종목 OHLCV 추정 거래대금 결합
- 외국인/기관 KRX+NXT 통합 순매수량을 Money Flow 계산에 연결
- KOSPI/KOSDAQ 시장지표 연결 및 5초 시세 갱신 구조 추가
- Timeline을 08:00~20:00으로 확장해 NXT 프리/애프터 구간까지 유지
- Docker Compose로 React+Nginx 프론트엔드와 Node.js 백엔드를 한 번에 실행하도록 구성
- Windows용 `START_WINDOWS.bat` / `START_WINDOWS.ps1` 원클릭 실행기 추가
- 비밀키는 로컬 `.env`와 백엔드 컨테이너에서만 사용하고 프론트엔드에는 포함하지 않도록 분리
- 토스 Open API 허용 IP 오류를 화면에서 설명하도록 처리
- 백엔드 세션 단위 테스트, Docker 이미지 빌드 CI, 08:00~20:00 Playwright 검증 추가
- GitHub Pages는 비밀키 없는 정적 DEMO FALLBACK을 유지하고 실제 토스 데이터는 로컬 Docker에서 제공

## 0.3.0
- 한국 주식 섹터 Money Flow 전광판 메인 화면 추가
- KOSPI/KOSDAQ 지수, 상승/하락 종목 수, 거래대금, 외국인/기관 수급 요약 추가
- 섹터 Heatmap과 Money Flow Ranking 추가
- 섹터 → 세부테마 → 종목 Drill-down 탐색 추가
- 09:00~15:30 Timeline Replay 슬라이더 추가
- 등락률, 거래대금, 상승종목 비율, 외국인/기관 수급을 조합한 Money Flow Score 구현
- 전광판/트레이딩룸 스타일의 어두운 반응형 UI 적용
- 기존 종목 퀴즈는 상단 메뉴의 별도 화면으로 유지
- Playwright 브라우저 테스트와 Money Flow Score 단위 테스트 추가
- 현재 시장 데이터는 화면과 상호작용 검증용 DEMO SNAPSHOT이며 실시간 데이터 제공자 연결을 위한 구조로 분리 가능

## 0.2.0
- 종목명을 보고 4개의 기업 설명 중 정답을 고르는 주식 기업 맞히기 게임 추가
- 삼성전자, SK하이닉스, 현대차, NAVER, 한화에어로스페이스 샘플 문제 추가
- 정답/오답 피드백, 점수, 진행률, 결과 화면, 다시 도전하기 기능 추가
- 모바일 반응형 UI와 키보드 포커스 접근성 추가
- 퀴즈 정답 판정 단위 테스트와 Playwright 브라우저 테스트 추가

## 0.1.0
- 바이브코딩 기본 프로젝트 생성
- React + TypeScript + Vite 구성
- Vitest 단위 테스트 구성
- Playwright 브라우저 테스트 구성
- GitHub Actions CI 구성
- AI 자율 개발 규칙 및 프로젝트 메모 구조 추가
- 기본 3D 인터랙션 데모 추가