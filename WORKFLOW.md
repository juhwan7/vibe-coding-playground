# Chat-First Autonomous Development Workflow

이 프로젝트의 목표는 사용자가 **일반 ChatGPT 채팅에서만 명령**하고, 나머지 개발 과정은 최대한 자동으로 처리하는 것이다.

## 전체 흐름

사용자 자연어 요청
→ ChatGPT가 저장소/메모/코드 확인
→ 필요한 경우 최신 구현 방법 조사
→ 구현 또는 프로토타입 작성
→ GitHub에 반영
→ GitHub Actions 자동 실행
→ TypeScript / 단위 테스트 / 빌드 / Playwright 브라우저 테스트
→ 실패 시 로그 분석
→ 같은 접근을 반복하지 않고 원인별로 수정 또는 전략 변경
→ 새 커밋
→ CI 재검증
→ 성공 시 CHANGELOG/MEMORY 정리
→ 사용자에게 최종 결과만 간단히 전달

## 요청 처리 규칙

- 사용자는 개발 용어를 몰라도 된다.
- "3D처럼 만들어줘", "더 신기하게", "재밌는 기능 넣어줘" 같은 요청도 유효하다.
- 구현 세부사항은 AI가 판단한다.
- 큰 취향 선택이 아닌 기술 선택은 묻지 않는다.
- 가능하면 작은 프로토타입으로 빠르게 검증한 후 확대한다.

## 전략 전환 규칙

전략 A가 실패했을 때 동일 시도를 무한 반복하지 않는다.

1. 현재 접근 수정은 최대 2회
2. 같은 원인으로 계속 실패하면 원인 재분류
3. 공식 문서/최신 사례 확인
4. 다른 라이브러리 또는 구현 방식 검토
5. 기능을 작은 단위로 분해
6. 최소 재현 예제에서 검증
7. 성공한 방법만 본 프로젝트에 적용

## Git 운영

- 작은 안전한 수정은 main에 직접 반영할 수 있다.
- 큰 UI 개편, 라이브러리 교체, 구조 변경은 실험 브랜치 사용을 우선 고려한다.
- 실험이 실패하면 브랜치를 버리고 정상 상태를 유지한다.
- 사용자가 "아까 상태로 돌려"라고 하면 최근 정상 커밋을 기준으로 복구한다.

## 자동 테스트 정의

CI는 최소한 다음을 확인한다.

1. TypeScript 정적 검사
2. Vitest 단위 테스트
3. Vite 프로덕션 빌드
4. Playwright 실제 브라우저 테스트
5. 데스크톱/모바일 화면 산출물

테스트 실패는 재실행 횟수가 아니라 **원인을 바꿔 해결했는지**를 중요하게 본다.

## 사용자에게 묻지 않는 것

- 파일 위치
- 함수명
- 내부 구조
- 작은 디자인 세부사항
- 무료 라이브러리 선택
- 일반적인 오류 수정 방법
- 테스트 추가 여부

## 사용자에게 확인하는 것

- 돈이 드는 서비스
- 로그인/비밀키/인증정보
- 중요한 데이터 삭제
- 보안 위험
- 서비스 공개 범위나 개인정보 관련 결정
- 프로젝트 방향이 완전히 달라지는 큰 선택

## 현재 한계

일반 ChatGPT는 연결된 GitHub와 GitHub Actions를 통해 저장소 수정과 원격 테스트를 수행할 수 있다. 사용자의 Windows PC 자체를 항상 직접 조작하는 것은 별도 로컬 실행 연결이 없으면 불가능하다.

따라서 현재 기본 실행 환경은 GitHub Actions이며, 로컬 PC 조작이 실제로 필요해지는 기능은 이후 MCP 또는 다른 안전한 로컬 실행 연결을 추가해 확장한다.
